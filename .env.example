# Oracle connection
ORACLE_HOST=
ORACLE_PORT=
ORACLE_SERVICE=
ORACLE_USER=
ORACLE_PASSWORD=
# Required when connecting as SYS. Accepted values: SYSDBA, SYSOPER
# ORACLE_PRIVILEGE=SYSDBA

# Schema and table pattern to monitor (the tables Debezium will capture)
CAPTURE_SCHEMA=
CAPTURE_TABLE_PATTERN=.*_DEF$

# Sampling interval in minutes (default 15)
SAMPLE_INTERVAL_MINUTES=15
