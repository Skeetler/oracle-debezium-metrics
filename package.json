{
  "name": "dbz-oracle-diag",
  "version": "1.0.0",
  "description": "Debezium Oracle CDC diagnostic tool — profiles a database and recommends optimal configuration",
  "scripts": {
    "build": "tsc",
    "setup": "ts-node src/index.ts setup",
    "report": "ts-node src/index.ts report",
    "teardown": "ts-node src/index.ts teardown"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "dotenv": "^16.4.0",
    "oracledb": "^6.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/oracledb": "^6.10.1",
    "ts-node": "^10.9.0",
    "typescript": "^5.4.0"
  }
}
